# DevKnowledge 进度记录

> 更新时间：2026-06-06（第七轮）

---

## 一、前端项目搭建

从零创建了完整的 React 前端项目（33 个文件）。


| 技术栈             | 版本 |
| ------------------ | ---- |
| React              | 19   |
| TypeScript         | 5.8  |
| Vite               | 6.3  |
| Tailwind           | v4   |
| Zustand            | 5.0  |
| react-router-dom   | 7.6  |
| eventsource-parser | 3.0  |

**目录结构：**

```
frontend/
├── package.json / vite.config.ts / tsconfig*.json / index.html
└── src/
    ├── main.tsx / App.tsx / index.css / vite-env.d.ts
    ├── api/          # client + auth + knowledge + demos + skills + settings + kb + embedding
    ├── components/   # layout + knowledge + skills + Antigravity + ShapeBlur
    ├── hooks/        # useSSE
    ├── pages/        # Home + Login + Knowledge + Demo + Skills + Kb + Settings
    │   └── settings/ # AiSettings + EmbeddingSettings + StorageSettings（设置子页面）
    ├── storage/      # LocalActivityStorage (File System Access API)
    ├── stores/       # authStore (Zustand) + particleContext
    └── types/        # api.ts
```

---

## 二、全部页面（中文界面）


| 页面        | 路由         | 功能                                                             |
| ----------- | ------------ | ---------------------------------------------------------------- |
| 首页        | `/`          | 四模块入口卡片 + ShapeBlur 形状模糊悬停效果                      |
| 登录/注册   | `/login`     | 表单 + 登录/注册切换                                             |
| 知识搜索    | `/knowledge` | 框架浏览 + 全文搜索（防抖）+ 深链接跳转                          |
| Demo 生成   | `/demos`     | SSE 流式输出 + 框架/语言选择 + 历史记录                          |
| Skills 构建 | `/skills`    | 手动提取 + 推荐 Tab + 编辑/采纳/忽略                             |
| 知识库      | `/kb`        | CRUD + 文档上传 + 语义搜索                                       |
| 设置        | `/settings`  | 顶部导航栏切换：AI 配置（多服务商管理 + Token 消耗图）/ 数据存储 |

---

## 三、前端 UI 特效

### 3.1 Antigravity 粒子光标效果

- 全局背景层，`position: fixed`，`pointer-events: none`
- 基于 Three.js + @react-three/fiber 的 InstancedMesh 粒子系统
- 使用全局 `window.addEventListener('mousemove')` 追踪鼠标（Canvas 不接收事件）
- 鼠标悬停侧边栏时粒子淡出（React Context 控制 opacity 过渡）
- 配色 `#cbc4e8`，opacity 0.3，autoAnimate 自动运动

### 3.2 ShapeBlur 形状模糊效果

- 首页四张卡片，hover 时显现 ShapeBlur 背景
- 使用自定义 shader（RoundRect SDF），形状边界对齐卡片边缘
- `shapeSize=2.1` 对齐卡片边界，`u_aspect` 按宽高比拉伸 X 轴
- 每张卡片使用不同的 variation（0-3），鼠标移动时形状产生模糊变形
- 颜色 `#2563eb`（primary-600），opacity 0.7

---

## 四、后端项目搭建

### 4.1 技术选型


| 技术         | 版本   | 说明                                             |
| ------------ | ------ | ------------------------------------------------ |
| Spring Boot  | 3.3    | WebFlux 响应式 Web 层                            |
| Java         | 17     | LTS                                              |
| MyBatis Plus | 3.5.7  | 阻塞式 ORM，用`Schedulers.boundedElastic()` 包装 |
| PostgreSQL   | 16     | 全文搜索 + pgvector                              |
| Flyway       | -      | 数据库迁移                                       |
| JJWT         | 0.12.6 | JWT 生成/解析                                    |
| Lombok       | -      | 简化代码                                         |

### 4.2 项目结构

```
backend/
├── pom.xml
└── src/main/
    ├── java/com/devknowledge/
    │   ├── DevKnowledgeApplication.java
    │   ├── config/
    │   │   ├── SecurityConfig.java          # WebFlux Security（放行公开接口）
    │   │   ├── CorsConfig.java              # CORS 允许 localhost:5173
    │   │   └── MyBatisPlusConfig.java       # 注册 UUID + String[] TypeHandler
    │   ├── config/
    │   │   ├── SecurityConfig.java          # WebFlux Security（放行公开接口）
    │   │   ├── CorsConfig.java              # CORS 允许 localhost:5173
    │   │   ├── MyBatisPlusConfig.java       # 注册 UUID + String[] TypeHandler
    │   │   └── GlobalExceptionHandler.java  # 全局异常处理（RuntimeException → JSON）
    │   ├── controller/
    │   │   ├── AuthController.java          # 注册/登录/刷新/profile
    │   │   ├── SettingsController.java      # 多 AI 配置 CRUD + 切换 + 删除 + token 统计
    │   │   ├── EmbeddingConfigController.java # Embedding 配置 CRUD + 测试 + Token 统计
    │   │   ├── KnowledgeController.java     # 框架列表/链接列表/全文搜索
    │   │   └── DemoController.java          # Demo SSE 生成 + 历史 CRUD
    │   ├── service/
    │   │   ├── AuthService.java             # 认证服务
    │   │   ├── AiConfigService.java         # AI 配置服务（含真实连通性测试）
    │   │   ├── EmbeddingConfigService.java  # Embedding 配置服务
    │   │   ├── EmbeddingService.java        # Embedding API 调用（OpenAI /v1/embeddings）
    │   │   ├── EmbeddingUsageService.java   # Embedding Token 消耗统计
    │   │   ├── KnowledgeService.java        # 知识搜索服务
    │   │   └── DemoService.java             # Demo 生成服务（ReAct Agent + 工具）
    │   ├── mapper/
    │   │   ├── UserMapper.java
    │   │   ├── UserAiConfigMapper.java
    │   │   ├── UserEmbeddingConfigMapper.java
    │   │   ├── FrameworkMapper.java
    │   │   ├── KnowledgeLinkMapper.java     # 含 fullTextSearch 自定义 SQL
    │   │   ├── KbChunkMapper.java           # 含 searchByVector + updateVectorById
    │   │   └── EmbeddingUsageMapper.java
    │   ├── model/
    │   │   ├── User.java
    │   │   ├── UserAiConfig.java
    │   │   ├── UserEmbeddingConfig.java     # Embedding 配置实体
    │   │   ├── Framework.java
    │   │   ├── KnowledgeLink.java
    │   │   ├── KnowledgeBase.java           # 新增 embeddingModel/embeddingDimensions
    │   │   ├── KbDocument.java              # 新增 chunkCount
    │   │   ├── KbChunk.java                 # 文档切片实体（pgvector）
    │   │   ├── EmbeddingUsage.java          # Token 消耗实体
    │   │   ├── UuidTypeHandler.java         # UUID ↔ PostgreSQL uuid
    │   │   └── StringArrayTypeHandler.java  # String[] ↔ PostgreSQL text[]
    │   ├── dto/
    │   │   ├── LoginRequest / RegisterRequest / AuthResponse / UserResponse
    │   │   ├── AiConfigRequest / AiConfigResponse / ProviderInfo
    │   │   ├── EmbeddingConfigRequest / EmbeddingConfigResponse
    │   │   ├── KbChunkSearchResult
    │   │   └── LinkSearchResult
    │   └── security/
    │       ├── JwtTokenProvider.java        # JWT 生成/解析（access 2h / refresh 7d）
    │       ├── JwtAuthenticationFilter.java # WebFlux Filter
    │       └── AesUtil.java                 # AES-256-GCM 加密/脱敏
    └── resources/
        ├── application.yml
        └── db/migration/
            ├── V1__create_users.sql             # users 表
            ├── V2__create_user_ai_configs.sql   # user_ai_configs 表
            ├── V3__create_frameworks_and_links.sql  # frameworks + knowledge_links + 全文索引
            ├── V4__seed_data.sql                # 5 个框架 + 28 条知识链接
            └── V8__rag_vector_search.sql        # pgvector + kb_chunks + embedding 配置表
```

---

## 五、Phase 2：Demo 生成 + ReAct Agent ✅

### 5.1 AI 适配层


| 文件                              | 说明                                                              |
| --------------------------------- | ----------------------------------------------------------------- |
| `ai/AiProviderAdapter.java`       | 统一适配器接口（streamCompletion + streamWithTools）              |
| `ai/AiProviderFactory.java`       | 按 provider 名称路由到对应适配器                                  |
| `ai/OpenAiCompatibleAdapter.java` | OpenAI 兼容格式实现（支持 DeepSeek/小米/自定义）                  |
| `ai/AiFunction.java`              | 工具定义（name + description + parametersJson）                   |
| `ai/AiChunk.java`                 | 输出块（TEXT / THOUGHT / TOOL_CALL / TOOL_RESULT / DONE / ERROR） |
| `ai/AiChunkType.java`             | 输出块类型枚举                                                    |
| `ai/ToolHandler.java`             | 工具执行函数接口                                                  |

### 5.2 ReAct Agent 引擎

- **全响应式实现**：使用 `Sinks.Many` + `subscribe()` 递归调用，兼容 Netty 事件循环
- **多轮推理**：最多 3 轮，AI 自主决定是否调用工具
- **工具执行**：`search_links`（搜索知识库）+ `get_framework_info`（获取框架信息）
- **实时推送**：每个 chunk 通过 SSE 即时推送到前端

### 5.3 Demo 服务

- **流式生成**：`DemoController` → `DemoService` → `ReActAgent` → `OpenAiCompatibleAdapter`
- **前置检查**：HTTP 400 拦截未登录/未配置 AI 的请求
- **自动保存**：流式完成后在 `doOnComplete` 中提取代码块和关键词存入 `demos` 表
- **历史记录**：已登录用户可查看/删除历史 Demo

### 5.4 前端 Demo 页面

- **SSE 流式渲染**：useSSE Hook 解析 thought / tool_call / text / done / error 事件
- **ReAct 可视化**：思考过程（蓝色卡片）、工具调用（琥珀色卡片）
- **代码输出**：Markdown 风格渲染，代码块带语言标签 + `max-h-80` 可滚动
- **错误通知**：集成 useNotify 全局弹窗

### 5.5 数据库


| 表      | 迁移文件 | 说明                                                                |
| ------- | -------- | ------------------------------------------------------------------- |
| `demos` | V5       | Demo 记录（user_id, title, prompt, code_content, language, tags[]） |

---

---

## 五、后端 API 接口清单

### 认证（公开）


| Method | Path                 | 说明             |
| ------ | -------------------- | ---------------- |
| POST   | `/api/auth/register` | 注册（返回 JWT） |
| POST   | `/api/auth/login`    | 登录（返回 JWT） |
| POST   | `/api/auth/refresh`  | 刷新 token       |

### 用户（需登录）


| Method | Path                                 | 说明                           |
| ------ | ------------------------------------ | ------------------------------ |
| GET    | `/api/user/profile`                  | 获取当前用户信息               |
| GET    | `/api/user/ai-config`                | 获取激活的 AI 配置（key 脱敏） |
| GET    | `/api/user/ai-configs`               | 获取所有 AI 配置列表           |
| PUT    | `/api/user/ai-config`                | 创建/更新 AI 配置（自动激活）  |
| POST   | `/api/user/ai-configs/{id}/activate` | 切换激活配置                   |
| DELETE | `/api/user/ai-configs/{id}`          | 删除配置                       |
| POST   | `/api/user/ai-config/test`           | 测试激活配置连通性             |
| GET    | `/api/user/token-usage`              | 近 7 天 Token 消耗统计         |

### 知识搜索（公开）


| Method | Path                                      | 说明                       |
| ------ | ----------------------------------------- | -------------------------- |
| GET    | `/api/frameworks`                         | 框架列表                   |
| GET    | `/api/frameworks/{slug}/links`            | 框架下的知识链接           |
| GET    | `/api/links/search?q=xxx`                 | 全文搜索（tsvector + GIN） |
| GET    | `/api/links/search?q=xxx&framework=react` | 框架内全文搜索             |

### Demo 生成（需登录）


| Method | Path                  | 说明                             |
| ------ | --------------------- | -------------------------------- |
| POST   | `/api/demos/generate` | SSE 流式生成 Demo（ReAct Agent） |
| GET    | `/api/demos`          | 获取当前用户 Demo 历史列表       |
| GET    | `/api/demos/{id}`     | 获取单个 Demo 详情               |
| DELETE | `/api/demos/{id}`     | 删除 Demo                        |

### 服务商（公开）


| Method | Path             | 说明                 |
| ------ | ---------------- | -------------------- |
| GET    | `/api/providers` | 支持的 AI 服务商列表 |

### Wiki 知识图谱（需登录）

| Method | Path                       | 说明                        |
| ------ | -------------------------- | --------------------------- |
| POST   | `/api/wiki/upload`         | 上传单个文档                |
| POST   | `/api/wiki/upload-vault`   | 上传 Obsidian Vault 目录    |
| POST   | `/api/wiki/import/{kbDocId}` | 从知识库导入文档          |
| GET    | `/api/wiki/pages`          | 获取页面列表（支持分类筛选）|
| GET    | `/api/wiki/page?path=xxx`  | 读取 wiki 页面内容          |
| GET    | `/api/wiki/graph`          | 获取图谱数据（实体+关系）   |
| POST   | `/api/wiki/analyze/{docId}`| 手动触发深度分析            |
| POST   | `/api/wiki/lint`           | Wiki 健康检查               |
| DELETE | `/api/wiki/doc/{docId}`    | 删除文档及相关内容          |

### Embedding 配置管理（需登录）

| Method | Path                               | 说明                         |
| ------ | ---------------------------------- | ---------------------------- |
| GET    | `/api/user/embedding-config`       | 获取激活的 Embedding 配置    |
| GET    | `/api/user/embedding-configs`      | 获取所有 Embedding 配置列表  |
| PUT    | `/api/user/embedding-config`       | 创建/更新 Embedding 配置     |
| POST   | `/api/user/embedding-configs/{id}/activate` | 切换激活配置       |
| DELETE | `/api/user/embedding-configs/{id}` | 删除配置                    |
| POST   | `/api/user/embedding-config/test`  | 测试连通性                   |
| GET    | `/api/user/embedding-usage`        | 近 7 天 Embedding Token 消耗 |

---

## 六、数据库表


| 表                | 迁移文件 | 说明                                          |
| ----------------- | -------- | --------------------------------------------- |
| `users`           | V1       | 用户表（email UNIQUE）                        |
| `user_ai_configs` | V2       | AI 配置表（user_id UNIQUE，api_key AES 加密） |
| `frameworks`      | V3       | 框架表（slug UNIQUE）                         |
| `knowledge_links` | V3       | 知识链接表（tsvector 全文索引 + GIN）         |
| `demos`           | V5       | Demo 记录（tokens_used 已填充估算值）         |
| `user_ai_configs` | V6       | 多配置支持（新增 name、is_active 字段）       |
| `kb_chunks`              | V8       | 文档切片表（pgvector vector(1536) + HNSW 索引）|
| `user_embedding_configs` | V8       | 用户 Embedding AI 配置（API Key AES 加密）    |
| `embedding_usage`        | V8       | Embedding Token 消耗统计                      |
| `wiki_documents`         | V10      | Wiki 原始文档表                               |
| `wiki_entities`          | V10      | Wiki 实体表（LLM 提取）                       |
| `wiki_relations`         | V10      | Wiki 关系表（实体间关联）                     |
| `wiki_index`             | V10      | Wiki 索引表（页面元数据）                     |

---

## 七、修复的问题


| 问题                                            | 修复                                                    |
| ----------------------------------------------- | ------------------------------------------------------- |
| React 19`useRef()` 不传参报错                   | `useRef<T                                               |
| `import path from 'path'` ESM 不可用            | Vite alias 改用`/src`                                   |
| `keepExisting` 属性不存在                       | 改为`keepExistingData`                                  |
| `writable.size` 不存在                          | 先`getFile()` 再取 `file.size`                          |
| `idb` 包导入失败                                | 去掉依赖，改用原生 IndexedDB API                        |
| `LocalActivityStorage` 是 class                 | 改为纯函数导出                                          |
| `@types/wicg-file-system-access` 缺失           | 手动安装类型定义                                        |
| Antigravity Canvas 遮挡侧边栏点击               | `pointer-events: none` + 全局 mousemove                 |
| ShapeBlur 白色不可见                            | 添加`u_color` uniform，默认蓝色                         |
| ShapeBlur 形状未对齐卡片边界                    | `shapeSize=2.1` + `u_aspect` 宽高比拉伸                 |
| `IdType.ASSIGN_UUID` 与 `java.util.UUID` 不兼容 | 改为`IdType.INPUT` + 手动生成 UUID                      |
| MyBatis Plus 缺少 UUID TypeHandler              | 自定义`UuidTypeHandler` + 全局注册                      |
| PostgreSQL text[] 无 TypeHandler                | 自定义`StringArrayTypeHandler`                          |
| `IdType.ASSIGN_UUID` 生成 String 而非 UUID      | 改为`IdType.INPUT` + `UUID.randomUUID()`                |
| MyBatis Plus 无 UUID TypeHandler                | 自定义`UuidTypeHandler` + 全局注册                      |
| AES key 长度 46 字节不合法                      | 使用 SHA-256 从 jwt.secret 派生 32 字节密钥             |
| Flyway 校验和不匹配（V4 种子数据修改后）        | `clean-on-validation-error: true`                       |
| `blockLast()` 阻塞 Netty 线程                   | 重写 ReActAgent 为`Sinks.Many` + `subscribe()` 递归模式 |
| 小米 API 不支持`role: "tool"`                   | 改用`assistant` + `user` 角色传递工具结果               |
| `reasoning_content` 被当作回复输出              | 只输出`content` 字段，跳过 `reasoning_content`          |
| useSSE 吞掉错误不抛出                           | catch 块中加`throw error`                               |
| SSE 流中错误事件无法返回                        | 改为 HTTP 400 前置检查 + GlobalExceptionHandler         |
| useSSE cancel() 未关联 AbortController 到 fetch | 重写 hook，signal 参数透传到 fetch 请求                 |
| 手动 SSE 行解析不够健壮                         | 引入 eventsource-parser 的 EventSourceParserStream      |
| 工具调用参数在流式分片中丢失                    | 按 tool_call index 累积 name + arguments，兼容无 index 模型 |
| 历史 Demo 点击后代码无样式                      | codeContent 加 markdown 围栏包裹                         |
| demos 表始终为空                                | 在`doOnComplete` 回调中调用 `saveDemoSync()` 保存       |
| AI 连接测试只检查字段非空                       | 改为真正发送 API 请求验证连通性                         |
| 连接测试首个 chunk 内容为空                     | 添加`.filter(chunk -> !chunk.isEmpty())`                |
| ReAct Agent 硬编码 3 轮无法适配不同复杂度      | 动态 maxIterations（默认 5，可配置 1-8）               |
| 工具返回空结果仍浪费后续轮次                    | 分层兜底：连续 2 轮全失败才硬停止，首次失败继续推理  |
| 模型反复调同一工具导致死循环                    | 死循环检测：记录 (fnName, args) 签名，重复即停止      |
| Demo 详情接口缺少用户归属校验                   | getDemo 加 userId 参数，非归属用户返回 403             |
| WebFlux 文件上传 @RequestParam 不生效            | 改用 @RequestPart + FilePart 原生方式                  |
| AI 配置 isActive 字段 JSON 序列化为 active       | @JsonProperty("isActive") 强制字段名                   |
| 新建 AI 配置唯一约束冲突                        | insert 前先 deactivateAll 取消旧激活配置               |
| 空响应 body 导致 JSON 解析报错                   | client.ts 先 text() 判断空再 parse                     |
| AI 配置页点击列表项自动触发激活                   | 分离选中和激活，新增「设为默认」按钮                   |
| KbDocument 缺少 chunkCount 字段                   | 模型新增 chunkCount 字段（V8 迁移 + 实体同步）       |

---

## 八、待办事项

### Phase 1：基础架构 ✅

- [X]  前端 React 项目搭建（33 文件）
- [X]  后端 Spring Boot 项目搭建
- [X]  Flyway 数据库迁移（V1-V5）
- [X]  种子数据（5 框架 + 35+ 知识链接）
- [X]  Auth 模块（注册/登录/JWT/刷新）
- [X]  AI 配置模块（CRUD + AES 加密 + 真实连通性测试）
- [X]  知识搜索模块（全文搜索 tsvector + GIN + Bing 网页搜索）
- [X]  全局异常处理 + 错误通知弹窗
- [X]  UI 特效（Antigravity 粒子 + ShapeBlur 形状模糊）

### Phase 2：Demo 生成 + ReAct Agent ✅

- [X]  AI 适配层（AiProviderAdapter + Factory + OpenAiCompatibleAdapter）
- [X]  ReAct Agent 引擎（Sinks.Many 全响应式，多轮推理）
- [X]  工具系统（search_links + get_framework_info）
- [X]  Demo SSE 流式生成 + 自动保存
- [X]  前端 ReAct 可视化（思考/工具调用/代码输出）
- [X]  AI 连接真实测试（发送 API 请求验证）

### Phase 2.5：设置页重构 ✅

- [X]  V6 迁移：多 AI 配置支持（name、is_active 字段）
- [X]  AiConfigService 多配置 CRUD（列表、切换、删除、激活保护）
- [X]  SettingsController 新接口（列表、切换、删除、token 统计）
- [X]  DemoService token 估算（字符数 × 1.5）+ 自动记录
- [X]  前端 SettingsPage 拆分：顶部导航栏 + AiSettings / StorageSettings 子页面
- [X]  AiSettings：我的 AI 列表 + 配置表单（model 无 placeholder）+ Token 消耗柱状图

### Phase 2.6：知识搜索改进 ✅

- [X]  框架内全文搜索：后端 `/links/search` 新增 `framework` 可选参数
- [X]  KnowledgeLinkMapper 新增 `fullTextSearchByFramework`（SQL 加 `framework_id` 条件）
- [X]  框架详情页加搜索框，结果限定当前框架
- [X]  搜索结果底部"搜索全部框架"按钮，可展开到全局搜索

### Phase 2.7：SSE 前端重写 ✅

- [X]  引入 `eventsource-parser` 替代手动 SSE 协议解析
- [X]  `client.ts` 改用 `EventSourceParserStream` + `pipeThrough` 模式
- [X]  `useSSE` hook 重写：AbortController 真正关联 fetch 请求，cancel() 可中断流
- [X]  组件卸载自动 abort，避免内存泄漏
- [X]  API 层适配（demos/skills 柯里化传入 signal）
- [X]  页面层适配（DemoPage/SkillsPage 调用方式更新）

### Phase 2.8：Code Review 修复 ✅

- [X]  `OpenAiCompatibleAdapter` 工具调用参数分片累积：按 `tool_call index` 累积 `name` + `arguments`，兼容无 index 字段的模型（小米等）
- [X]  历史 Demo 渲染修复：`codeContent` 加 markdown 围栏，解决点击历史后代码块无样式的问题
- [X]  Demo 输出区始终显示关闭按钮

### Phase 2.9：ReAct Agent 多轮推理优化 ✅

- [X]  动态轮数：默认 5 轮，API 可配置（1-8），`maxIterations` 参数
- [X]  提前终止：死循环检测（同工具同参数重复调用）、连续 2 轮全失败硬停止
- [X]  兜底策略：单轮工具失败不中断，错误信息注入对话让模型自行决定
- [X]  边界修正：maxIterations 超范围自动修正（1 ≤ n ≤ 8）
- [X]  ChatMessage record 扩展 `name` 字段，为 tool role 标准格式做准备
- [X]  OpenAiCompatibleAdapter 消息列表支持 `name` 字段
- [X]  System prompt 注入工具使用规则，引导模型自主控制调用节奏

### Phase 2 遗留问题 🔧

- [X]  **[P1] OpenAI 工具调用参数会在流式分片中丢失**：已按 tool call index 累积 name + arguments，兼容无 index 字段的模型
- [X]  **[P1] Demo 详情缺少用户归属校验**：GET /api/demos/{id} 已加 userId 校验，非归属用户返回 403
- [X]  **Demo 列表分页 + 关键词搜索**：后端 MyBatis Plus 分页 + title/prompt/language 模糊匹配，前端搜索框 + 翻页控件，默认每页 6 条
- [X]  **Demo Tags Jieba 分词优化**：jieba-analysis 替代硬编码关键词，停用词过滤，搜索增加 tags 匹配
- [X]  **多轮推理优化**：从硬编码 3 轮改为动态可配置（默认 5 轮），加入死循环检测和分层兜底策略
- [X]  **Demo Tags Jieba 分词优化**：jieba-analysis 替代硬编码关键词，停用词过滤，搜索增加 tags 匹配
- [X]  **对于demo存储的时候，tag字段的存储只是去碰撞codeKeywords常用关键字，如何优化呢？jieba分词是否可以用到？**



### Phase 3a：知识库基础架构 ✅

- [X]  Flyway V7 迁移：`knowledge_bases` + `kb_documents` 表
- [X]  实体模型 + Mapper + DTO
- [X]  FileParserService：支持 txt/md/pdf/docx 文件解析为纯文本
- [X]  KbService：知识库 CRUD + 文档上传 + 异步解析（3 线程池）+ LIKE 搜索
- [X]  KbController：REST API（创建/列表/详情/删除/上传/批量上传/搜索）
- [X]  前端适配：status 状态标签、pdf/docx 格式支持、搜索结果展示
- [X]  知识库介入 Demo 生成：可选 kbId，动态注入 search_kb 工具到 ReActAgent

### Phase 3b：RAG 向量检索 ✅

- [X]  Flyway V8 迁移：pgvector 扩展 + kb_chunks + user_embedding_configs + embedding_usage 表
- [X]  EmbeddingService：调用 OpenAI /v1/embeddings，支持批量 + 维度填充
- [X]  EmbeddingConfigService + Controller：Embedding AI 配置 CRUD + 测试 + Token 统计
- [X]  EmbeddingUsageService：记录文档入库和检索查询的 Token 消耗
- [X]  KbService 改造：段落切分（\n\n 分割 + 合并短段 + 截断长段）+ 批量向量化
- [X]  KbChunkMapper：pgvector 余弦相似度检索（HNSW 索引）
- [X]  DemoService：混合 RAG 预检索注入（top-K chunk 注入 system prompt + search_kb 工具二次检索）
- [X]  DemoToolProvider：search_kb 改用向量检索，返回相关度和来源
- [X]  前端 SettingsPage：侧边栏导航（AI 服务配置 / Embedding AI / 数据存储）
- [X]  前端 EmbeddingSettings 页面：API 凭证管理 + 测试连接 + Token 消耗柱状图
- [X]  前端 KbPage：创建知识库时选择 Embedding 模型和维度（创建后锁定）
- [X]  前端 DemoPage：Top-K 滑块（选择知识库时显示）

### Phase 3c：Wiki 知识图谱 ✅

基于 Karpathy LLM Wiki 模式，构建 LLM 驱动的知识图谱。

**后端（17 个文件）：**

- [X]  Flyway V10 迁移：wiki_documents + wiki_entities + wiki_relations + wiki_index 表
- [X]  Model 实体：WikiDocument / WikiEntity / WikiRelation / WikiIndex
- [X]  Mapper 接口：4 个 Mapper（无外键约束，Service 层控制引用完整性）
- [X]  DTO：WikiUploadResponse / WikiGraphResponse
- [X]  WikiFileService：vault 目录管理 + 文件读写（Schedulers.boundedElastic）
- [X]  WikiLlmService：LLM 分析（实体提取、关系识别、页面生成、Lint 健康检查）
- [X]  WikiIngestService：文档摄取（存储 + LLM 分析 + 生成 wiki 页面）
- [X]  WikiGraphService：图谱数据查询
- [X]  WikiRetrievalService：Demo Wiki 检索（基于索引的关键词匹配）
- [X]  WikiController：REST API（9 个端点：上传/Vault 上传/导入/页面列表/页面内容/图谱/分析/Lint/删除）

**前端（6 个新文件 + 4 个修改）：**

- [X]  类型定义：wiki.ts（WikiDocument/WikiEntity/WikiRelation/WikiIndexEntry/WikiGraphData 等）
- [X]  API 客户端：api/wiki.ts（上传/Vault 上传/页面列表/页面内容/图谱/分析/Lint/删除）
- [X]  WikiPage.tsx：主页面（侧边栏 + 内容区 + 图谱标签页 + Lint 结果展示）
- [X]  WikiUpload.tsx：上传组件（单文件 + Obsidian Vault 目录上传）
- [X]  GraphNode.tsx：3D 图谱节点（icosahedron 几何体 + 悬浮动画 + 标签）
- [X]  WikiGraph3D.tsx：3D 知识图谱（圆形布局 + 边渲染 + 节点交互）
- [X]  App.tsx：添加 /wiki 路由
- [X]  HomePage.tsx：添加 Wiki 入口卡片
- [X]  Sidebar.tsx：添加 Wiki 知识图谱导航项
- [X]  DemoPage.tsx：检索源切换（RAG/Wiki/无），RAG 模式需选择知识库

**配置：**

- [X]  application.yml 添加 wiki.vault.path 配置
- [X]  安装 @react-three/drei 依赖

### Phase 3c 待改进 🔧

- [X]  **MD 文档切分优化** — 已使用 flexmark-java AST 切分，按标题层级精确拆分（见 Phase 3c 文档解析优化）
- [ ]  **Wiki 检索优化** — 当前是简单关键词匹配，后续可优化为语义匹配或混合检索
- [ ]  **Obsidian Vault 目录上传** — 前端 webkitdirectory 属性兼容性测试

### Phase 3c 文档解析优化 ✅

- [X]  **大文件流式处理** — 文件 < 1MB 直接用 `FileParserService.parseText()`；> 1MB 使用 `BufferedReader` 流式解析（65536 缓冲区），避免一次性加载全部内容到堆内存
- [X]  **flexmark-java 拆分** — 使用 flexmark-java AST 替代自定义状态机，`MarkdownChunker` 按标题层级精确拆分 MD 文档，合并短段落，拆分长段落，代码块原样保留
- [X]  **文档解析重试** — `RawFileStorageService` 将原始文件持久化到磁盘（`raw_files/{kbId}/{docId}/`），`KbService.retryDocument()` 从磁盘重新读取并解析，新增 `POST /api/kb/documents/{docId}/retry` 端点，前端显示"重试"按钮 + 错误信息展示

### Phase 3c Embedding 维度统一 ✅

- [X]  **统一 1024 维** — `EmbeddingService` 移除旧 `VECTOR_DIMENSION=1536` 常量和 `padToTargetDimension`，新增 `VECTOR_DIMENSION=1024`；V11 迁移将 `kb_chunks.embedding` 改为 `vector(1024)` + 重建 HNSW；`knowledge_bases` 去掉 `embedding_dimensions` 列；前端移除维度输入框；`EmbeddingServiceTest` 9 个测试
- [X]  **存量数据迁移** — V11 迁移清空旧 1536 维向量，标记 `ready` 文档为 `pending` 待重新向量化

### Phase 3c RAG 混合检索优化 ✅

- [X]  **BM25 关键词检索** — V12 迁移新增 `tsv tsvector` 列 + GIN 索引；`JiebaSegmenter` 工具类（SEARCH 模式分词 + 中英文停用词过滤）；入库时 `chunkAndEmbed` 自动分词存入 tsv；`KbChunkMapper.searchByBm25` 使用 `ts_rank` + `@@` 检索；新增 `GET /{id}/search/bm25` 端点；`JiebaSegmenterTest` 15 个测试
- [X]  **RRF 融合排序** — `RrfRanker` 工具类实现 `merge(list1, list2, k, topN, idExtractor)`；`KbService.searchKbVector` 改为双通道检索（BM25 top-20 + Embedding top-20）→ RRF(k=60) 融合 → topK；`RrfRankerTest` 18 个测试
- [X]  **Qwen3-Reranker 精排** — `RerankerService` 调用 `POST {baseUrl}/rerank` API 精排；V13 迁移创建 `user_reranker_configs` 表；`RerankerConfigService` 完整 CRUD + AES 加密；`RerankerConfigController` REST 端点；集成到 `searchKbVector`（RRF 后可选精排，失败优雅降级）；`RerankerServiceTest` 8 个测试

**三阶段检索链路：BM25 关键词 → Embedding 向量 → RRF 融合 → Reranker 精排**

### Phase 4：Skills 构建器 + 智能推荐 🔲

### Phase 4：Skills 构建器 + 智能推荐 🔲

- [ ]  行为数据采集（本地存储）
- [ ]  关键词提取 + Skills 自动生成
- [ ]  基于历史行为的智能推荐

### Phase 5：完善 🔲

- [ ]  更多框架种子数据
- [ ]  分页 + 缓存优化
- [ ]  OpenAPI 文档
- [ ]  性能调优
- [ ]  **考虑接入 Spring AI**：替换自研 AiProviderAdapter 层
- [ ]  **目前系统还没有进行大规模测试，如果后续用户在使用的时候出现bug，我想能不能将报错相关摘要信息发送到管理员邮箱，方便后续排查问题**
